{
  "name": "aggregator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "shared": "workspace:*"
  }
}
