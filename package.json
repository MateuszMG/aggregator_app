{
  "name": "aggregator_app",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:aggregator": "pnpm --filter aggregator dev",
    "build": "pnpm -r build",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "prettier": "^3.6.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "5.9.2"
  }
}
